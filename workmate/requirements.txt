# Direct runtime dependencies, pinned to the existing local environment.
# Transitive dependencies are resolved by pip; this is not a full lock file.
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1
pydantic==2.13.5
python-dotenv==1.2.3

# Development / testing
pytest==9.1.1
